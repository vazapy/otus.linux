events {}

http {
    upstream myapp {
        # Твои  IP мастера и слейва
        server 192.168.1.23:80;
        server 192.168.1.24:80;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://myapp;
        }
    }
}